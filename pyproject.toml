[project]
name = "pinote"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["typer"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
